「これで完璧！」と思いたいところだけれど、数千万〜数億件規模の並行処理（Concurrency）と実運用には、**まだ絶対に警戒すべき4大リスク**が潜んでいるわ！

設計・実装で事故を起こさないために、以下の落とし穴と回避策をしっかり頭に叩き込んでおきなさい！

---

### 1. 並行処理における4大リスク

| リスク要因 | 発生メカニズム | 影響 | 回避策 |
| --- | --- | --- | --- |
| ① デッドロック（Deadlock） | 子への`INSERT`（親に`KEY SHARE`ロック）と、親の`DELETE CASCADE`（親・子に排他ロック）が同時に別トランザクションで交差実行された場合に発生。 | どちらかのトランザクションが強制ロールバック | 削除対象キーへの新規登録を止めるか、バッチ側でリトライ制御を実装 |
| **② 削除バッチの並列化事故** | チャンク分割削除をマルチスレッド等で「並列実行」した際、PKのソート順（探索順）が揃っていないと、同一テーブル内でスレッド同士がデッドロック。 | 削除バッチが異常終了 | 削除バッチは**単一スレッド（逐次実行）**にするか、並列化するなら**都道府県（パーティション）単位で完全分離** |
| **③ 子テーブルへのロック・I/O集中** | 親テーブルは48パーティションに分散しているが、**子テーブル8つが単一テーブル**の場合、全47都道府県の削除・参照・登録が子テーブルの同一B-Treeインデックスに殺到。 | バッファ競合（Buffer Pinning）やラッチ競合による全体遅延 | 子テーブルも都道府県でパーティショニングするか、I/O性能（IOPS）に余裕を持たせる |
| ④ MVCCのxmin滞留（ゴースト肥大化） | 削除バッチや重い集計処理が長時間トランザクション（数十分〜数時間）を保持し続けると、**DB全体の`VACUUM`がゴミ（Dead Tuple）を回収できなくなる**。 | テーブル・インデックスが急激に肥大化（Bloat）し、全クエリが遅延 | 1チャンクごとに必ず`COMMIT`を発行し、1トランザクションを数ミリ秒〜数秒で終わらせる |

---

### 2. 見落としがちな運用の落とし穴

* **レプリケーション遅延（リードレプリカ / スタンバイ機への影響）**
親の`DELETE CASCADE`で数百万件の子レコードが消えると、プライマリDB上は分割コミットしていても、**大量のWALレコードがスタンバイDBに一気に転送**されるわ。スタンバイ側のWAL適用が追いつかず、リードレプリカで参照遅延（Replication Lag）やクエリキャンセル（`max_standby_streaming_delay`超過）が多発する恐れがあるわよ。
* **パーティションプルーニング（絞り込み）の効き忘れ**
主テーブルに対する検索や削除処理で、WHERE句にパーティションキー（都道府県コード等）を含め忘れると、**48パーティションすべてを走査して全パーティションにロックを要求**してしまうわ。クエリには必ずパーティションキーを明示させなさい。
* **子テーブルのレアケース（1親に10万〜20万件）時の瞬間凍結**
通常（子3〜5件）は一瞬で終わるけれど、「1つの親に子レコードが20万件紐づいているレコード」を消す瞬間に当たると、その1件の親を消すだけで**20万行分のインデックス更新とWAL書き込みが1トランザクション内で一気に走る**わ。この特定の1件だけ処理時間が数秒〜十数秒跳ね上がるため、アプリ側のタイムアウト値（`statement_timeout`）を短くしすぎているとここで落ちるわよ。

---

### 3. 並行処理・運用を破綻させないための「鉄則ルール」

1. **削除バッチは都道府県（パーティションキー）単位で直列に処理する**
全国一括でランダムに消すのではなく、「東京都の削除バッチ」「大阪府の削除バッチ」のようにパーティション単位でスコープを絞り、1スレッドずつ小分けに`DELETE` ＋ `COMMIT`を回すのが最も安全よ。
2. **タイムアウト設定を適切に分離する**
オンラインAPI（Web画面）は短め（例: 3〜5秒）、夜間バッチ・削除処理は長め（例: 60秒以上）に `statement_timeout` / `lock_timeout` をセッション単位で切り替えなさい。ロック待ちによる全体の道連れ停止を防げるわ。
3. **レプリケーション監視（WAL Lag）を組み込む**
大量削除バッチを回す際は、レプリケーション遅延量（バイト数または秒数）を監視し、遅延が一定値を超えたら削除ループ内に `pg_sleep(1)` などのウェイトを挟んでスタンバイ機に息継ぎをさせる制御を入れておくと完璧よ。

これで懸念点・並行処理の盲点まで完全に網羅できたわ！自信を持って設計を進めなさい！
