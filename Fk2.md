これまでのすべての前提条件と追加情報を網羅し、そのまま関係者やユーザーへの提案・設計資料として提出できる形に整理したわ！

---

# 【技術検討資料】PostgreSQL 16.2における複合主キー外部キー制約（ON DELETE CASCADE）の設計・運用評価

## 1. 検討の前提条件

### 1.1. システム環境およびテーブル構成

* **データベース**: PostgreSQL 16.2
* **主テーブル**:
* **構成**: 47都道府県 ＋ デフォルトパーティション（計48固定パーティション）
* **主キー（PK）**: 9項目の複合キー（`ItemA` 〜 `ItemI`）※パーティションキーを含む
* **データ量**: 合計 数千万レコード
* **更新特性**: 一般項目の更新頻度は高めだが、**PK（9項目）の更新は発生しない**


* **関連テーブル（子テーブル × 8）**:
* **主キー（PK）**: 10項目以上の複合キー（`ItemA` 〜 `ItemI` ＋ `SEQ` 等）
* **外部キー（FK）**: 先頭9項目（`ItemA` 〜 `ItemI`）で主テーブルを参照
* **データ量**: 各テーブル数千万レコード（8テーブル合計で数億レコード規模）
* **データ関連性**: 主テーブル1レコードに対し、子テーブル各行は平均3〜5レコード（特定テーブルで＋数百件、レアケースで10万〜20万レコードが紐づく）
* **更新特性**: 原則として新規登録以降は更新されない



### 1.2. 運用・削除要件

* **削除頻度と規模**:
* 通常業務: 数件〜数百件単位
* まとまった削除: 数万〜数百万件（最大で親基準千万件規模の可能性あり）


* **パーティション運用**: 都道府県単位などのパーティション丸ごと削除（`DETACH` / `DROP`）は想定しない。
* **夜間メンテナンス**: 毎日夜間に最長2時間程度の定期メンテナンス（`VACUUM` / `REINDEX`）を実施。

---

## 2. 検討結果・推奨方針（エグゼクティブサマリー）

```
【結論】
9項目複合キーによる外部キー制約（ON DELETE CASCADE）の採用を「強く推奨」します。

ただし、数万件を超える大量削除については、1SQLでの一括DELETEを禁止し、
アプリケーション/バッチ側で「1,000〜5,000件刻みのチャンク分割削除」を行う運用設計とします。

```

### 採用判断の要点

1. **データ整合性の完全担保**: 8つの巨大関連テーブルに対し、孤児レコード発生をDB層で100%防止可能。
2. **通常時の性能オーバーヘッドがほぼゼロ**:
* 子テーブルのPKインデックス（先頭9列）がFK探索にそのまま活用されるため、FKチェックによるフルスキャンは発生しない。
* 主テーブルのPK更新が発生しないため、日々の更新処理における子テーブルへの制約チェック負荷が極めて軽微。


3. **メンテナンスによる品質担保**: 夜間の定期`VACUUM` / `REINDEX`により、削除後のインデックス肥大化や統計情報の劣化を確実にリセット可能。

---

## 3. メリット・デメリット比較

| 評価項目 | 外部キー制約あり（`ON DELETE CASCADE`） | 外部キー制約なし（アプリ側制御） | 本環境における評価 |
| --- | --- | --- | --- |
| **データ整合性** | **完全保証**。DB層で孤児レコードの混入を100%防止 | **脆弱**。削除失敗・例外時に不整合データが残るリスクあり | **FKありの優位性が極めて高い** |
| **子テーブル探索性能** | 子テーブルPKの前方一致インデックスが効くため高速 | 同等 | **問題なし** |
| **主テーブル更新負荷** | 非PK更新ならHOT更新が効くためオーバーヘッド極小 | 最速 | **問題なし**（PK更新がないため） |
| **通常削除（〜数百件）** | `DELETE FROM parent` 1発で連鎖削除が即座に完了 | 子8テーブル＋親を順次消すコードの実装が必要 | **開発・運用保守コストを大幅削減** |
| **大量削除（数万件〜）** | 1SQL実行だと内部で数千万行のロック・WAL急増が発生 | 同様に高負荷（分割制御が必要） | **バッチ分割削除の実装でリスク回避可能** |

---

## 4. 性能および内部挙動の詳細分析

### ① 削除処理の対象レコード数（物理インパクト）の定義

`ON DELETE CASCADE` による削除コストは、主テーブルだけでなく「連鎖して削除される全子テーブルの合計レコード数」で決定されます。

* **例：主テーブルを10万件削除する場合**
* 主テーブル: 10万件
* 子テーブル8つ: 平均4行 × 8テーブル × 10万件 ＝ **約320万件**
* レアケース（1つの親に10万行保有する子が混在）: ＋ 数十万件
* **合計物理削除数: 330万〜400万件以上**


* 内部的には1レコードずつ削除マーク（Dead Tuple化）とWAL書き込みが発生するため、1トランザクションで一気に流すと長時間の行排他ロック（`RowExclusiveLock`）とWALフラッシュによるI/Oスパイクを引き起こします。

### ② 子テーブルのインデックス効率

子テーブルの主キー定義が `(ItemA, ItemB, ..., ItemI, SEQ)` のように、**先頭に主テーブルのPK 9項目が並んでいること**が前提となります。

* この定義順であれば、PostgreSQLのB-Treeインデックスの前方一致探索が機能し、FK参照チェックおよびCASCADE削除時の探索でフルスキャン（Seq Scan）を完全に回避（Index Scan）できます。

### ③ 主テーブル更新（UPDATE）時の挙動

* 主テーブルの更新時、更新対象カラムにPK（9項目）が含まれていなければ、PostgreSQLは子テーブルへの整合性チェックをスキップします。
* これにより **HOT（Heap-Only Tuple）更新** が機能し、インデックスの書き換えや子テーブルへの問い合わせオーバーヘッドなしに高速更新が維持されます。

### ④ 夜間メンテナンス（VACUUM / REINDEX）による効果

* 大量削除によって発生したテーブル・インデックスの空き領域（Dead Tuple / Bloat）は、夜間の2時間メンテナンス枠で確実に回収・再構築されます。
* これにより、長期運用における検索性能の劣化を恒常的に防ぐことができます。

---

## 5. 推奨実装方針と運用設計

### 5.1. DDL定義規約（必須順序）

子テーブル作成時、複合主キーの列順序を厳密に守る必要があります。

```sql
-- 主テーブル
CREATE TABLE parent_table (
    item_a VARCHAR(32) NOT NULL,
    item_b VARCHAR(32) NOT NULL,
    -- ... item_c 〜 item_h ...
    pref_code INT NOT NULL, -- パーティションキー（ItemI相当）
    data_value TEXT,
    CONSTRAINT pk_parent PRIMARY KEY (item_a, item_b, ..., pref_code)
) PARTITION BY LIST (pref_code);

-- 子テーブル（8テーブル共通の設計原則）
CREATE TABLE child_table_1 (
    item_a VARCHAR(32) NOT NULL,
    item_b VARCHAR(32) NOT NULL,
    -- ... item_c 〜 item_h ...
    pref_code INT NOT NULL,
    seq INT NOT NULL,
    sub_data TEXT,
    -- 必ず先頭9列を親のPK順と一致させてPKを定義する
    CONSTRAINT pk_child_1 PRIMARY KEY (item_a, item_b, ..., pref_code, seq),
    CONSTRAINT fk_child_1 FOREIGN KEY (item_a, item_b, ..., pref_code)
        REFERENCES parent_table (item_a, item_b, ..., pref_code)
        ON DELETE CASCADE
);

```

### 5.2. 大量削除バッチの実装パターン（チャンク分割）

数万件以上のデータを削除する際は、単一の巨大な`DELETE`文を発行せず、以下のように**1,000〜5,000件刻みで`COMMIT`を実行するバッチ構成**を採用します。

```sql
-- 削除対象の親PKを一時テーブルまたはカーソルで取得し、ループ処理で分割実行
DO $$
DECLARE
    deleted_count INT;
BEGIN
    LOOP
        -- 1回あたり1,000件の親レコードを対象に削除（ON DELETE CASCADEにより子も安全に連鎖削除）
        WITH target AS (
            SELECT item_a, item_b, ..., pref_code
            FROM parent_table
            WHERE delete_flag = TRUE -- 削除対象条件
            LIMIT 1000
            FOR UPDATE SKIP LOCKED
        )
        DELETE FROM parent_table p
        USING target t
        WHERE p.item_a = t.item_a
          AND p.item_b = t.item_b
          -- ...
          AND p.pref_code = t.pref_code;

        GET DIAGNOSTICS deleted_count = ROW_COUNT;

        -- 対象がなくなったら終了
        EXIT WHEN deleted_count = 0;

        -- 1,000件ごとにトランザクションをコミットしてロックとWALを解放
        COMMIT;
    END LOOP;
END $$;

```

---

## 6. 結論・チェックリスト

| 確認項目 | 設計・運用基準 | 判定 |
| --- | --- | --- |
| **FK制約（ON DELETE CASCADE）** | 採用する | **合意** |
| **子テーブルPK定義順** | 先頭に親PKの9項目を配置し、末尾に`SEQ`等を配置する | **必須** |
| **主テーブルPKの更新** | アプリケーション仕様としてPK更新を行わない | **合意** |
| **通常削除（〜数百件）** | 親テーブルへの通常の`DELETE`文を実行（CASCADE連鎖） | **即時実行可** |
| **大量削除（数万件〜）** | 1,000〜5,000件単位で`COMMIT`を刻むバッチ処理とする | **必須** |
| **日次メンテナンス** | 夜間に定期的な`VACUUM`および`REINDEX`を実行する | **実施予定** |

---

これでクライアントやチーム内への説明も完璧ね！
この構成なら、整合性を100%守りつつ、パフォーマンス事故も確実に回避できるわ。自信を持って提案してらっしゃい！
